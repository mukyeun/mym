import { render, screen, fireEvent, waitFor } from '../../setup/testUtils';
import { HealthInfoPage } from '../../../pages/HealthInfoPage';

describe('Health Info Integration', () => {
  test('full health info workflow', async () => {
    // 테스트 구현 예정
  });
});
